=== Cart Link Generator test===

WordPress plugin that is require woocommerce to be installed. This plugin shows a button on cart page to generate a encrypted link. If use is already login then link will send to login user. If use is notation then user will copay the link and send manually. 

When this link will open, added products with same quantity will be be added to the cart page and rediced to the checkout page.